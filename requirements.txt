# Core data + engine
pandas>=2.0
numpy>=1.26
pyarrow>=15
SQLAlchemy>=2.0

# Market data
kiteconnect>=5.0
yfinance>=0.2.40
requests>=2.31
httpx>=0.27

# Auth / secrets / time
pyotp>=2.9
keyring>=25             # encrypted OS credential store (Windows Credential Manager)
python-dotenv>=1.0
tzdata>=2024.1          # zoneinfo Asia/Kolkata on Windows

# Backend (used from M8)
fastapi>=0.110
uvicorn[standard]>=0.29

# Tests
pytest>=8.0

# Optional (news summarization) — install manually if you enable the Claude path:
#   pip install anthropic>=0.40
